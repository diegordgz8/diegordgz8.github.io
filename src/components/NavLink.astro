---
interface Props {
    href: string;
    label: string;
}

const { href, label } = Astro.props;
---

<a
    class="nav-link w-full hover:text-brand dark:hover:text-brand-dark transition-colors duration-300"
    href={href}
    data-section={href.replace('/#', '')}
>
    {label}
</a>
