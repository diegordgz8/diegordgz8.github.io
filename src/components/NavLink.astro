---
interface Props {
    href: string;
    label: string;
}

const { href, label } = Astro.props;
---

<a
    class="nav-link hover:text-brand dark:hover:text-brand-dark w-full transition-colors duration-300"
    href={href}
    data-section={href.replace('/#', '')}
>
    {label}
</a>
